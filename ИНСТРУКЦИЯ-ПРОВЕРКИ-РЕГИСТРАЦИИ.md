# Инструкция по проверке проблемы с регистрацией

## 🔴 Описание проблемы

Клиент запрашивает регистрацию, администратор одобряет, клиент получает уведомление:
```
✅ Ваша регистрация одобрена!
Теперь вы можете создавать заявки.
При создании первой заявки вам нужно будет указать ваше имя и телефон.
Отправьте /start для начала работы.
```

Но после `/start` снова идет запрос на регистрацию вместо показа меню.

## 🔍 Диагностика

### Шаг 1: Проверьте базу данных

Запустите скрипт проверки:

```bash
cd telegram-order-bot
node check-real-clients.js
```

Этот скрипт покажет:
- ✅ Всех клиентов в базе данных
- ⏳ Ожидающие запросы на регистрацию
- 📊 Статус конкретных клиентов

**Что проверить:**
1. Есть ли клиент в таблице `clients`?
2. Активен ли клиент (`is_active = 1`)?
3. Есть ли у клиента ожидающий запрос (`status = pending`)?

### Шаг 2: Проверьте логи бота

После добавления логирования, запустите бота и попросите клиента отправить `/start`.

В логах вы увидите:

```
🔍 Команда /start от пользователя 123456789 (Имя Клиента)
   isAdmin: false
   isClient: true  <-- Должно быть true!
   ✅ Показываем клиентское меню
```

**Если `isClient: false`:**
- Клиент НЕ найден в таблице `clients`
- Или `is_active = 0`
- Проверьте базу данных (Шаг 1)

**Если `isClient: true`:**
- Клиент найден и активен
- Должно показаться меню "🏬 Склад"
- Если меню не показывается, проблема в другом месте

### Шаг 3: Тест полного процесса регистрации

Запустите тест:

```bash
cd telegram-order-bot
node test-registration-flow.js
```

Этот тест симулирует полный процесс:
1. Создание запроса на регистрацию
2. Одобрение администратором
3. Проверка, что клиент добавлен
4. Проверка, что `isClient` возвращает `true`

**Если тест проходит:**
- Код работает правильно
- Проблема может быть в:
  - Кэшировании
  - Использовании старой версии базы данных
  - Боте не перезапущен после изменений

**Если тест не проходит:**
- Есть проблема в коде
- Проверьте функцию `approveClient` в `database.js`

## ✅ Решения

### Решение 1: Перезапустите бота

После одобрения клиента, перезапустите бота:

```bash
# Остановите бота (Ctrl+C)
# Запустите снова
node bot.js
```

### Решение 2: Проверьте базу данных

Убедитесь, что используется правильная база данных:

```bash
# Проверьте путь к базе данных в .env
DB_PATH=./orders.db
```

Проверьте, что файл `orders.db` существует и не поврежден.

### Решение 3: Добавьте клиента вручную

Если автоматическое одобрение не работает, добавьте клиента вручную:

```
/addclient Telegram_ID | Имя | Телефон
```

Пример:
```
/addclient 123456789 | Алишер Иванов | +992901234567
```

### Решение 4: Проверьте статус запроса

Возможно, запрос остался в статусе `pending`. Проверьте:

```bash
node check-real-clients.js
```

Если запрос в статусе `pending`, одобрите его снова через админ-панель.

## 🔧 Дополнительные проверки

### Проверка 1: Прямой запрос к базе данных

```bash
sqlite3 orders.db "SELECT * FROM clients WHERE telegram_id = 123456789;"
```

Замените `123456789` на реальный Telegram ID клиента.

**Ожидаемый результат:**
```
id|telegram_id|name|phone|added_by|is_active|created_at
1|123456789|Имя Клиента|+992900000000|5889669586|1|2026-02-07 10:00:00
```

**Если результат пустой:**
- Клиент НЕ добавлен в базу данных
- Проблема в функции `approveClient`

**Если `is_active = 0`:**
- Клиент был заблокирован
- Разблокируйте клиента или добавьте заново

### Проверка 2: Проверка запросов на регистрацию

```bash
sqlite3 orders.db "SELECT * FROM registration_requests WHERE telegram_id = 123456789;"
```

**Ожидаемый результат:**
```
id|telegram_id|name|username|status|created_at
1|123456789|Имя Клиента|username|approved|2026-02-07 10:00:00
```

**Если `status = pending`:**
- Запрос не был обработан
- Одобрите запрос снова

**Если запроса нет:**
- Запрос был удален или не создан
- Попросите клиента отправить `/start` снова

## 📋 Чек-лист диагностики

- [ ] Запустил `check-real-clients.js` - клиент найден в базе?
- [ ] Проверил логи бота - `isClient` возвращает `true`?
- [ ] Запустил `test-registration-flow.js` - тест проходит?
- [ ] Перезапустил бота после одобрения клиента?
- [ ] Проверил путь к базе данных в `.env`?
- [ ] Проверил, что `is_active = 1` для клиента?
- [ ] Проверил, что запрос в статусе `approved`?

## 🎯 Быстрое решение

Если ничего не помогает, добавьте клиента вручную:

1. Узнайте Telegram ID клиента (он показывается при запросе на регистрацию)
2. Отправьте команду:
   ```
   /addclient ID | Имя | Телефон
   ```
3. Попросите клиента отправить `/start`
4. Клиент должен увидеть меню "🏬 Склад"

## 📞 Если проблема сохраняется

1. Соберите информацию:
   - Telegram ID клиента
   - Логи бота при `/start`
   - Результат `check-real-clients.js`
   - Результат `test-registration-flow.js`

2. Проверьте:
   - Версию Node.js: `node --version` (должна быть 14+)
   - Версию SQLite: `sqlite3 --version`
   - Права доступа к файлу `orders.db`

3. Попробуйте:
   - Удалить файл `orders.db` и создать заново (ВНИМАНИЕ: потеряете все данные!)
   - Переустановить зависимости: `npm install`
   - Обновить код из репозитория

---

**Дата создания:** 7 февраля 2026  
**Версия:** 1.0
