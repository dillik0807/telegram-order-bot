# 🔧 Пошаговая инструкция по исправлению ошибки БД

## Ваша ошибка
```
❌ Ошибка подключения к БД: SQLITE_CANTOPEN
❌ TypeError: Cannot set properties of undefined (setting 'softDeleteOrder')
```

## Решение за 4 шага

### Шаг 1: Проверьте тип базы данных

```bash
cd telegram-order-bot
npm run check-db
```

Вы увидите какая БД используется (PostgreSQL или SQLite).

### Шаг 2: Переключитесь на PostgreSQL

```bash
npm run use-postgres
```

Это автоматически:
- ✅ Создаст резервную копию текущего database.js
- ✅ Установит PostgreSQL версию
- ✅ Добавит pg в package.json
- ✅ Обновит .env файл

### Шаг 3: Установите зависимости

```bash
npm install
```

Это установит драйвер PostgreSQL (`pg`).

### Шаг 4: Настройте DATABASE_URL

#### Для Railway:
Railway автоматически предоставляет `DATABASE_URL` при добавлении PostgreSQL сервиса.
Просто добавьте PostgreSQL в ваш проект Railway.

#### Для локальной разработки:
Откройте `.env` и обновите:
```env
DATABASE_URL=postgresql://postgres:password@localhost:5432/orders
```

Замените:
- `postgres` - ваш пользователь PostgreSQL
- `password` - ваш пароль
- `localhost` - хост (или IP адрес)
- `5432` - порт PostgreSQL
- `orders` - название базы данных

### Шаг 5: Проверьте подключение (опционально)

```bash
npm run test-postgres
```

Вы должны увидеть:
```
✅ Подключение успешно!
✅ Тест завершен успешно!
```

### Шаг 6: Запустите бота

```bash
npm start
```

Вы должны увидеть:
```
✅ PostgreSQL подключен
✅ Таблицы PostgreSQL инициализированы
🔧 Используется PostgreSQL - исправления встроены
🤖 Бот запущен успешно!
📱 Telegram: @your_bot_username
```

## Если что-то пошло не так

### Ошибка: "Cannot find module 'pg'"
```bash
npm install pg
```

### Ошибка: "password authentication failed"
Проверьте DATABASE_URL - неверный пароль или пользователь.

### Ошибка: "connection refused"
PostgreSQL сервер не запущен или неверный хост/порт.

### Ошибка: "database does not exist"
Создайте базу данных:
```sql
CREATE DATABASE orders;
```

## Полезные команды

```bash
# Проверить конфигурацию БД
npm run check-db

# Тест подключения к PostgreSQL
npm run test-postgres

# Переключиться на PostgreSQL
npm run use-postgres

# Вернуться к SQLite
npm run use-sqlite

# Запустить бота
npm start

# Запустить с автоперезагрузкой
npm run dev
```

## Для Railway

1. Откройте ваш проект на Railway
2. Нажмите "New" → "Database" → "Add PostgreSQL"
3. Railway автоматически создаст переменную `DATABASE_URL`
4. Выполните команды выше
5. Сделайте деплой

## Возврат к SQLite

Если нужно вернуться к SQLite:

```bash
npm run use-sqlite
```

Обновите `.env`:
```env
DB_PATH=./orders.db
```

## Дополнительная помощь

- Быстрое решение: [FIX-DATABASE-ERROR.md](./FIX-DATABASE-ERROR.md)
- Подробная настройка: [POSTGRESQL-SETUP.md](./POSTGRESQL-SETUP.md)
- Что исправлено: [ИСПРАВЛЕНО-БД.md](./ИСПРАВЛЕНО-БД.md)

## Контакты

Если проблема не решена, предоставьте:
1. Вывод команды `npm run check-db`
2. Логи ошибки при запуске бота
3. Используете ли Railway/Heroku или локально
