# 🔴 Проблема: База данных обнуляется после деплоя на Railway

## Описание проблемы

После каждого деплоя (redeploy) на Railway:
- ❌ Весь список клиентов исчезает
- ❌ Все заявки удаляются
- ❌ Ничего не сохраняется

**Причина:** SQLite база данных (`orders.db`) хранится в файловой системе контейнера, которая **эфемерна** (временная). При каждом деплое контейнер пересоздается и все данные теряются.

## ✅ Решение 1: Использовать Railway Volume (Рекомендуется)

Railway Volume - это персистентное хранилище, которое сохраняется между деплоями.

### Шаг 1: Создайте Volume в Railway

1. Откройте ваш проект на Railway
2. Перейдите в настройки сервиса (Settings)
3. Найдите раздел "Volumes"
4. Нажмите "New Volume"
5. Настройте Volume:
   - **Mount Path:** `/app/data`
   - **Name:** `telegram-bot-data`

### Шаг 2: Обновите путь к базе данных

Измените файл `.env` или добавьте переменную окружения в Railway:

```env
DB_PATH=/app/data/orders.db
```

### Шаг 3: Обновите код для создания директории

Добавьте в начало `database.js`:

```javascript
const fs = require('fs');
const path = require('path');

// Создаем директорию для базы данных, если её нет
const dbPath = process.env.DB_PATH || './orders.db';
const dbDir = path.dirname(dbPath);

if (!fs.existsSync(dbDir)) {
  fs.mkdirSync(dbDir, { recursive: true });
  console.log(`📁 Создана директория для БД: ${dbDir}`);
}
```

### Шаг 4: Деплой

После настройки Volume и обновления кода, сделайте деплой. Теперь база данных будет сохраняться между деплоями!

## ✅ Решение 2: Использовать PostgreSQL (Более надежно)

Для production рекомендуется использовать PostgreSQL вместо SQLite.

### Преимущества PostgreSQL:

- ✅ Персистентное хранилище
- ✅ Лучшая производительность
- ✅ Поддержка конкурентных запросов
- ✅ Резервное копирование
- ✅ Масштабируемость

### Шаг 1: Добавьте PostgreSQL в Railway

1. В вашем проекте Railway нажмите "New"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст базу данных
4. Скопируйте переменные окружения (DATABASE_URL)

### Шаг 2: Установите зависимости

```bash
npm install pg
npm install --save-dev @types/pg
```

### Шаг 3: Создайте адаптер для PostgreSQL

Создайте файл `database-postgres.js` (я создам его ниже)

### Шаг 4: Обновите переменные окружения

Добавьте в Railway:

```env
DATABASE_URL=postgresql://user:password@host:port/database
USE_POSTGRES=true
```

## ✅ Решение 3: Резервное копирование в облако (Дополнительно)

Даже с Volume рекомендуется делать резервные копии.

### Вариант A: Резервное копирование в Google Drive

Используйте библиотеку `googleapis` для автоматического бэкапа.

### Вариант B: Резервное копирование в AWS S3

Используйте `aws-sdk` для загрузки бэкапов в S3.

### Вариант C: Резервное копирование в Telegram

Отправляйте бэкап файл в Telegram канал или группу.

## 🔧 Быстрое решение (Временное)

Если нужно срочно сохранить данные:

### 1. Скачайте базу данных перед деплоем

Добавьте команду для скачивания БД:

```javascript
// В bot.js добавьте команду для администратора
bot.command('downloaddb', async (ctx) => {
  const userId = ctx.from.id;
  
  if (!admin.isAdmin(userId)) {
    return ctx.reply('❌ У вас нет прав администратора');
  }
  
  try {
    const dbPath = process.env.DB_PATH || './orders.db';
    
    await ctx.replyWithDocument(
      { source: dbPath, filename: `backup-${Date.now()}.db` },
      { caption: '💾 Резервная копия базы данных' }
    );
  } catch (error) {
    console.error('Ошибка скачивания БД:', error);
    ctx.reply('❌ Ошибка при скачивании базы данных');
  }
});
```

### 2. Загрузите базу данных после деплоя

Добавьте команду для загрузки БД:

```javascript
bot.command('uploaddb', async (ctx) => {
  const userId = ctx.from.id;
  
  if (!admin.isAdmin(userId)) {
    return ctx.reply('❌ У вас нет прав администратора');
  }
  
  ctx.reply('📤 Отправьте файл базы данных (.db)');
  
  // Обработчик будет добавлен ниже
});

// Обработчик загрузки файла
bot.on('document', async (ctx) => {
  const userId = ctx.from.id;
  
  if (!admin.isAdmin(userId)) {
    return;
  }
  
  const document = ctx.message.document;
  
  if (!document.file_name.endsWith('.db')) {
    return ctx.reply('❌ Файл должен иметь расширение .db');
  }
  
  try {
    const file = await ctx.telegram.getFile(document.file_id);
    const fileUrl = `https://api.telegram.org/file/bot${process.env.TELEGRAM_BOT_TOKEN}/${file.file_path}`;
    
    // Скачиваем файл
    const response = await fetch(fileUrl);
    const buffer = await response.arrayBuffer();
    
    // Сохраняем в файл
    const fs = require('fs');
    const dbPath = process.env.DB_PATH || './orders.db';
    
    fs.writeFileSync(dbPath, Buffer.from(buffer));
    
    ctx.reply('✅ База данных успешно загружена! Перезапустите бота.');
  } catch (error) {
    console.error('Ошибка загрузки БД:', error);
    ctx.reply('❌ Ошибка при загрузке базы данных');
  }
});
```

## 📋 Пошаговая инструкция (Railway Volume)

### 1. Настройка Volume в Railway

```
1. Откройте Railway → Ваш проект → Сервис бота
2. Settings → Volumes → New Volume
3. Mount Path: /app/data
4. Нажмите "Add"
```

### 2. Обновите переменные окружения

```
Railway → Ваш проект → Сервис бота → Variables
Добавьте:
DB_PATH=/app/data/orders.db
```

### 3. Обновите код

Я создам обновленный `database.js` с поддержкой Volume.

### 4. Деплой

```bash
git add .
git commit -m "Add Railway Volume support"
git push
```

Railway автоматически сделает деплой.

### 5. Проверка

После деплоя:
1. Добавьте тестового клиента
2. Сделайте redeploy
3. Проверьте, что клиент остался в базе

## ⚠️ Важные замечания

### О Railway Volumes:

- ✅ Данные сохраняются между деплоями
- ✅ Автоматическое резервное копирование
- ⚠️ Привязаны к региону (не переносятся между регионами)
- ⚠️ Могут быть удалены при удалении сервиса

### О SQLite на Railway:

- ✅ Простота использования
- ✅ Не требует дополнительных сервисов
- ⚠️ Ограничения по конкурентности
- ⚠️ Не подходит для высоких нагрузок

### Рекомендации:

1. **Для малых проектов:** Railway Volume + SQLite
2. **Для средних проектов:** PostgreSQL на Railway
3. **Для больших проектов:** PostgreSQL + резервное копирование

## 🔍 Диагностика

### Проверьте, используется ли Volume:

Добавьте команду для проверки:

```javascript
bot.command('checkdb', async (ctx) => {
  const userId = ctx.from.id;
  
  if (!admin.isAdmin(userId)) {
    return ctx.reply('❌ У вас нет прав администратора');
  }
  
  const fs = require('fs');
  const dbPath = process.env.DB_PATH || './orders.db';
  
  let message = '🔍 Информация о базе данных:\n\n';
  message += `📁 Путь: ${dbPath}\n`;
  
  if (fs.existsSync(dbPath)) {
    const stats = fs.statSync(dbPath);
    message += `✅ Файл существует\n`;
    message += `📊 Размер: ${(stats.size / 1024).toFixed(2)} KB\n`;
    message += `📅 Изменен: ${stats.mtime.toLocaleString('ru-RU')}\n`;
  } else {
    message += `❌ Файл не существует\n`;
  }
  
  const clients = await database.getAllClients();
  message += `\n👥 Клиентов в базе: ${clients.length}`;
  
  ctx.reply(message);
});
```

## 📞 Если проблема сохраняется

1. Проверьте, что Volume создан в Railway
2. Проверьте переменную окружения `DB_PATH`
3. Проверьте логи Railway на наличие ошибок
4. Убедитесь, что директория `/app/data` доступна для записи

---

**Дата создания:** 7 февраля 2026  
**Версия:** 1.0  
**Статус:** ✅ Решение готово
