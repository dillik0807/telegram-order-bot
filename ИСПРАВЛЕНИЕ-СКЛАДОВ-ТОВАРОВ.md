# Исправление добавления складов и товаров

## 🔴 Проблема

При добавлении складов и товаров могли возникать те же проблемы, что и с клиентами:
- ❌ Дубликаты не блокировались
- ❌ Данные могли исчезать
- ❌ Не было проверки существования

## ✅ Решение

Добавлена проверка на дубликаты перед добавлением складов и товаров, аналогично клиентам.

---

## 📋 Что изменено

### 1. Функция `addWarehouse` в `database.js`

**Было:**
```javascript
addWarehouse(name, whatsappGroupId = null) {
  return new Promise((resolve, reject) => {
    this.db.run(
      'INSERT INTO warehouses (name, whatsapp_group_id) VALUES (?, ?)',
      [name, whatsappGroupId],
      function(err) {
        if (err) return reject(err);
        resolve(this.lastID);
      }
    );
  });
}
```

**Стало:**
```javascript
addWarehouse(name, whatsappGroupId = null) {
  return new Promise((resolve, reject) => {
    // Проверяем, существует ли уже такой склад
    this.db.get(
      'SELECT * FROM warehouses WHERE name = ? AND is_active = 1',
      [name],
      (err, existingWarehouse) => {
        if (err) return reject(err);
        
        if (existingWarehouse) {
          // Склад уже существует
          console.log(`⚠️ Склад "${name}" уже существует (ID: ${existingWarehouse.id})`);
          const error = new Error(`Склад "${name}" уже существует`);
          error.code = 'WAREHOUSE_EXISTS';
          error.existingId = existingWarehouse.id;
          return reject(error);
        }
        
        // Склад не существует - добавляем
        this.db.run(
          'INSERT INTO warehouses (name, whatsapp_group_id) VALUES (?, ?)',
          [name, whatsappGroupId],
          function(err) {
            if (err) {
              console.error(`❌ Ошибка добавления склада "${name}":`, err);
              return reject(err);
            }
            console.log(`✅ Склад "${name}" добавлен (ID: ${this.lastID})`);
            resolve(this.lastID);
          }
        );
      }
    );
  });
}
```

### 2. Функция `addProduct` в `database.js`

**Было:**
```javascript
addProduct(name) {
  return new Promise((resolve, reject) => {
    this.db.run(
      'INSERT INTO products (name) VALUES (?)',
      [name],
      function(err) {
        if (err) return reject(err);
        resolve(this.lastID);
      }
    );
  });
}
```

**Стало:**
```javascript
addProduct(name) {
  return new Promise((resolve, reject) => {
    // Проверяем, существует ли уже такой товар
    this.db.get(
      'SELECT * FROM products WHERE name = ? AND is_active = 1',
      [name],
      (err, existingProduct) => {
        if (err) return reject(err);
        
        if (existingProduct) {
          // Товар уже существует
          console.log(`⚠️ Товар "${name}" уже существует (ID: ${existingProduct.id})`);
          const error = new Error(`Товар "${name}" уже существует`);
          error.code = 'PRODUCT_EXISTS';
          error.existingId = existingProduct.id;
          return reject(error);
        }
        
        // Товар не существует - добавляем
        this.db.run(
          'INSERT INTO products (name) VALUES (?)',
          [name],
          function(err) {
            if (err) {
              console.error(`❌ Ошибка добавления товара "${name}":`, err);
              return reject(err);
            }
            console.log(`✅ Товар "${name}" добавлен (ID: ${this.lastID})`);
            resolve(this.lastID);
          }
        );
      }
    );
  });
}
```

### 3. Обновлена команда `/addwarehouse2` в `admin.js`

Теперь использует новую логику проверки дубликатов из `database.js`:

```javascript
try {
  // Добавляем склад (функция сама проверит дубликаты)
  const warehouseId = await database.addWarehouse(name, null);
  
  // Обновляем данные в памяти
  await dataManager.loadWarehousesAndProducts();
  
  ctx.reply(`✅ Склад "${name}" успешно добавлен!`);
  
} catch (error) {
  if (error.code === 'WAREHOUSE_EXISTS') {
    ctx.reply(`⚠️ Склад "${name}" уже существует!`);
  } else {
    ctx.reply(`❌ Ошибка: ${error.message}`);
  }
}
```

### 4. Обновлена команда `/addproduct` в `admin.js`

Аналогично складам:

```javascript
try {
  // Добавляем товар (функция сама проверит дубликаты)
  const productId = await database.addProduct(name);
  
  // Обновляем данные в памяти
  await dataManager.loadWarehousesAndProducts();
  
  ctx.reply(`✅ Товар "${name}" успешно добавлен!`);
  
} catch (error) {
  if (error.code === 'PRODUCT_EXISTS') {
    ctx.reply(`⚠️ Товар "${name}" уже существует!`);
  } else {
    ctx.reply(`❌ Ошибка: ${error.message}`);
  }
}
```

### 5. Обновлен `data-manager.js`

Функции `addWarehouseAndReload` и `addProductAndReload` теперь правильно обрабатывают ошибки дубликатов:

```javascript
async function addWarehouseAndReload(name) {
  try {
    const warehouseId = await database.addWarehouse(name);
    await loadWarehousesAndProducts();
    return warehouseId;
  } catch (error) {
    // Если склад уже существует, просто обновляем данные
    if (error.code === 'WAREHOUSE_EXISTS') {
      await loadWarehousesAndProducts();
    }
    throw error;
  }
}
```

---

## 🧪 Тестирование

Создан тестовый скрипт `test-warehouses-products.js` для проверки:

```bash
node test-warehouses-products.js
```

### Что тестируется:

**Склады:**
1. ✅ Добавление нового склада
2. ✅ Проверка, что склад добавлен
3. ✅ Попытка добавить дубликат (должна быть заблокирована)
4. ✅ Проверка, что количество не изменилось

**Товары:**
1. ✅ Добавление нового товара
2. ✅ Проверка, что товар добавлен
3. ✅ Попытка добавить дубликат (должна быть заблокирована)
4. ✅ Проверка, что количество не изменилось

### Результат теста:

```
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!

✅ Склады:
   - Добавление работает
   - Дубликаты блокируются
   - Данные стабильны

✅ Товары:
   - Добавление работает
   - Дубликаты блокируются
   - Данные стабильны
```

---

## 📋 Команды для администратора

### Склады

```
/addwarehouse2 Название - Добавить склад
/checkwarehouses - Посмотреть все склады
/setwhatsapp Название | ID_группы - Настроить WhatsApp
/removewarehouse ID - Удалить склад
```

**Примеры:**
```
/addwarehouse2 Склад №5
/checkwarehouses
/setwhatsapp Склад №5 | 120363XXXXXXXXXX@g.us
```

### Товары

```
/addproduct Название - Добавить товар
📋 Список товаров - Посмотреть все товары (в админ-панели)
/removeproduct ID - Удалить товар
```

**Примеры:**
```
/addproduct Гравий
/removeproduct 5
```

---

## ✅ Результат

После исправления:

### Склады:
1. ✅ **Дубликаты блокируются** - нельзя добавить склад с существующим названием
2. ✅ **Понятные сообщения** - администратор видит, что склад уже существует
3. ✅ **Данные стабильны** - список складов не исчезает
4. ✅ **Логирование** - все операции записываются в лог

### Товары:
1. ✅ **Дубликаты блокируются** - нельзя добавить товар с существующим названием
2. ✅ **Понятные сообщения** - администратор видит, что товар уже существует
3. ✅ **Данные стабильны** - список товаров не исчезает
4. ✅ **Логирование** - все операции записываются в лог

---

## 🔍 Проверка

### Проверка складов:

1. **Попробуйте добавить новый склад:**
   ```
   /addwarehouse2 Тестовый Склад
   ```
   Должно: `✅ Склад "Тестовый Склад" успешно добавлен!`

2. **Попробуйте добавить тот же склад снова:**
   ```
   /addwarehouse2 Тестовый Склад
   ```
   Должно: `⚠️ Склад "Тестовый Склад" уже существует!`

3. **Проверьте список:**
   ```
   /checkwarehouses
   ```
   Склад должен быть в списке один раз

### Проверка товаров:

1. **Попробуйте добавить новый товар:**
   ```
   /addproduct Тестовый Товар
   ```
   Должно: `✅ Товар "Тестовый Товар" успешно добавлен!`

2. **Попробуйте добавить тот же товар снова:**
   ```
   /addproduct Тестовый Товар
   ```
   Должно: `⚠️ Товар "Тестовый Товар" уже существует!`

3. **Проверьте список:**
   ```
   📋 Список товаров (в админ-панели)
   ```
   Товар должен быть в списке один раз

---

## 📁 Измененные файлы

1. **telegram-order-bot/database.js**
   - Функция `addWarehouse` - добавлена проверка дубликатов
   - Функция `addProduct` - добавлена проверка дубликатов

2. **telegram-order-bot/admin.js**
   - Команда `/addwarehouse2` - обновлена обработка ошибок
   - Команда `/addproduct` - обновлена обработка ошибок

3. **telegram-order-bot/data-manager.js**
   - Функция `addWarehouseAndReload` - улучшена обработка ошибок
   - Функция `addProductAndReload` - улучшена обработка ошибок

4. **Новые файлы:**
   - `test-warehouses-products.js` - тестовый скрипт
   - `ИСПРАВЛЕНИЕ-СКЛАДОВ-ТОВАРОВ.md` - эта документация

---

## 🎯 Итоги

Теперь добавление складов и товаров работает так же надежно, как и добавление клиентов:

- ✅ **Нет дубликатов** - каждый склад/товар уникален
- ✅ **Понятные ошибки** - администратор видит, что пошло не так
- ✅ **Стабильные данные** - ничего не исчезает
- ✅ **Логирование** - все операции отслеживаются
- ✅ **Тестирование** - автоматические тесты проверяют работу

---

**Дата создания:** 7 февраля 2026  
**Версия:** 1.0  
**Статус:** ✅ Исправлено и протестировано
