# 🚀 Деплой функции редактирования заявок

## Быстрый старт

### 1. Проверка локально (опционально)

```bash
cd telegram-order-bot

# Проверить что колонки добавлены
node check-columns.js

# Запустить полный тест
node test-order-edit-complete.js
```

Если тесты прошли успешно - можно деплоить!

---

## 2. Деплой на Railway

### Шаг 1: Закоммитить изменения

```bash
# Из корневой папки проекта
git add .
git commit -m "feat: добавлена функция редактирования заявок с обновлением в Telegram и WhatsApp"
git push
```

### Шаг 2: Railway автоматически задеплоит

Railway обнаружит изменения и:
1. Скачает новый код
2. Установит зависимости
3. Запустит бота

### Шаг 3: Проверка после деплоя

1. Откройте бота в Telegram
2. Создайте тестовую заявку
3. Используйте `/myorders` чтобы увидеть заявки
4. Используйте `/editorder ID` для редактирования
5. Проверьте что сообщения обновились в группах

---

## 3. Миграция базы данных

### SQLite (локально)
Миграция уже выполнена командой `node check-columns.js`

### PostgreSQL (Railway)
Миграция выполнится автоматически при первом запуске бота после деплоя.

Проверить можно командой в боте:
```
/checkdb
```

---

## 4. Проверка работы

### Тест 1: Клиент редактирует свою заявку

1. Создайте заявку как клиент
2. Отправьте `/myorders`
3. Используйте `/editorder ID`
4. Измените транспорт
5. Сохраните изменения
6. Проверьте что сообщение обновилось в Telegram группе

### Тест 2: Администратор редактирует заявку

1. Откройте админ-панель: `/admin`
2. Выберите "📊 Статистика"
3. Выберите "📦 Последние заявки"
4. Используйте `/editorder ID` для любой заявки
5. Измените склад
6. Сохраните изменения
7. Проверьте что сообщение отправилось в новую WhatsApp группу

---

## 5. Что делать если что-то не работает

### Проблема: Команды не работают

**Решение:**
```bash
# Перезапустите бота на Railway
# В Railway Dashboard:
# 1. Откройте ваш проект
# 2. Нажмите "Restart"
```

### Проблема: Ошибка "column not found"

**Решение:**
```bash
# Если используете SQLite локально:
node check-columns.js

# Если используете PostgreSQL на Railway:
# Миграция должна выполниться автоматически
# Проверьте логи Railway
```

### Проблема: Сообщения не обновляются

**Причины:**
1. ID сообщений не сохранились (заявка создана до обновления)
2. Бот не имеет прав редактировать сообщения в группе

**Решение:**
1. Создайте новую заявку (после обновления)
2. Проверьте права бота в Telegram группе (должен быть администратором)

---

## 6. Команды для пользователей

### Для клиентов:
- `/myorders` - Мои заявки
- `/editorder ID` - Редактировать заявку

### Для администраторов:
- `/admin` - Админ-панель
- `/editorder ID` - Редактировать любую заявку
- Кнопка в "📦 Последние заявки"

---

## 7. Технические детали

### Новые колонки в базе данных:
```sql
telegram_message_id TEXT  -- ID сообщения в Telegram
whatsapp_message_id TEXT  -- ID сообщения в WhatsApp
telegram_group_id TEXT    -- ID Telegram группы
```

### Новые файлы:
- `order-edit-manager.js` - Менеджер редактирования
- `add-order-edit-feature.js` - Миграция
- `test-order-edit-complete.js` - Тесты
- `check-columns.js` - Проверка колонок

### Измененные файлы:
- `bot.js` - Команды /editorder и /myorders
- `admin.js` - Кнопка редактирования в списке
- `database-postgres.js` - Новые колонки

---

## ✅ Чек-лист деплоя

- [ ] Локальные тесты пройдены
- [ ] Изменения закоммичены
- [ ] Код запушен в Git
- [ ] Railway задеплоил новую версию
- [ ] Бот перезапустился
- [ ] Миграция выполнена (проверить `/checkdb`)
- [ ] Тестовая заявка создана
- [ ] Редактирование работает
- [ ] Сообщения обновляются

---

## 🎉 Готово!

После выполнения всех шагов функция редактирования заявок будет работать в продакшене.

**Важно:** Только заявки созданные ПОСЛЕ обновления будут иметь возможность редактирования сообщений. Старые заявки можно редактировать, но сообщения в группах не обновятся (т.к. ID не сохранены).
