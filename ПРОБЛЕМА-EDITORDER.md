# ❌ Проблема: /editorder не работает

## 🔍 Диагностика

Локально все тесты прошли успешно:
- ✅ Функция `getOrderWithItems` работает
- ✅ Модуль `OrderEditManager` загружается
- ✅ Команда `/editorder` найдена в `bot.js`
- ✅ Колонки БД добавлены

## 🎯 Возможные причины на Railway

### 1. Бот не перезапустился после деплоя

**Проверка:**
1. Откройте Railway Dashboard
2. Перейдите в Deployments
3. Проверьте статус последнего деплоя
4. Посмотрите логи - должно быть:
   ```
   🤖 Бот запущен успешно!
   📱 Telegram: @ваш_бот
   ```

**Решение:**
- Нажмите "Restart" в Railway Dashboard

---

### 2. Миграция БД не выполнилась (PostgreSQL)

**Проверка:**
Отправьте боту команду `/checkdb` (только для админов)

**Если используете PostgreSQL:**
- Колонки должны добавиться автоматически при первом запуске
- Проверьте логи Railway на наличие ошибок миграции

**Решение:**
Если колонки не добавились, выполните миграцию вручную:

```sql
-- Подключитесь к PostgreSQL на Railway
ALTER TABLE orders ADD COLUMN IF NOT EXISTS telegram_message_id TEXT;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS whatsapp_message_id TEXT;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS telegram_group_id TEXT;
```

---

### 3. Ошибка при выполнении команды

**Проверка логов Railway:**

Найдите ошибки связанные с:
- `OrderEditManager`
- `getOrderWithItems`
- `/editorder`

**Типичные ошибки:**

#### Ошибка 1: "Cannot find module './order-edit-manager'"
**Причина:** Файл не задеплоился на Railway

**Решение:**
```bash
# Убедитесь что файл закоммичен
git status
git add order-edit-manager.js
git commit -m "add: order-edit-manager.js"
git push
```

#### Ошибка 2: "getOrderWithItems is not a function"
**Причина:** Функция не экспортируется из database модуля

**Решение:** Проверьте что в `database.js` и `database-postgres.js` функция экспортируется

#### Ошибка 3: "column does not exist"
**Причина:** Миграция БД не выполнилась

**Решение:** Выполните миграцию вручную (см. пункт 2)

---

### 4. Команда не зарегистрирована

**Проверка:**
Отправьте боту `/help` или `/start` - проверьте что бот отвечает

**Если бот не отвечает:**
- Проверьте что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте что бот запущен (логи Railway)

**Если бот отвечает, но /editorder не работает:**
- Проверьте что команда добавлена в `bot.js`
- Проверьте логи на ошибки при регистрации команды

---

## 🔧 Быстрое решение

### Шаг 1: Проверьте деплой

```bash
# Убедитесь что все файлы закоммичены
git status

# Если есть незакоммиченные файлы
git add .
git commit -m "fix: добавлены все файлы для редактирования"
git push
```

### Шаг 2: Перезапустите бот на Railway

1. Откройте Railway Dashboard
2. Найдите ваш проект
3. Нажмите "Restart"
4. Дождитесь перезапуска
5. Проверьте логи

### Шаг 3: Проверьте миграцию

Отправьте боту (как админ):
```
/checkdb
```

Должно показать информацию о БД и наличие колонок.

### Шаг 4: Протестируйте команду

1. Создайте новую заявку через бота
2. Отправьте `/myorders`
3. Используйте `/editorder ID` где ID - номер заявки

---

## 📝 Что проверить в логах Railway

Ищите эти строки:

**При запуске:**
```
🤖 Бот запущен успешно!
📱 Telegram: @ваш_бот
```

**При миграции (PostgreSQL):**
```
✅ Колонка telegram_message_id добавлена
✅ Колонка whatsapp_message_id добавлена
✅ Колонка telegram_group_id добавлена
```

**При использовании команды:**
```
🔍 Команда /editorder от пользователя XXXXX
```

**Ошибки:**
```
❌ Ошибка: ...
```

---

## 🆘 Если ничего не помогает

### Вариант 1: Откатитесь к предыдущей версии

```bash
git log --oneline  # Найдите коммит до изменений
git revert HEAD    # Откатите последний коммит
git push
```

### Вариант 2: Проверьте локально

```bash
cd telegram-order-bot

# Запустите бота локально
node bot.js

# В другом терминале протестируйте
node test-editorder.js
```

Если локально работает, проблема в Railway деплое.

### Вариант 3: Проверьте переменные окружения

На Railway должны быть установлены:
- `TELEGRAM_BOT_TOKEN` ✅
- `DATABASE_URL` (для PostgreSQL) ✅
- `TELEGRAM_GROUP_ID` ✅
- Все `WHATSAPP_*` переменные ✅

---

## 📞 Дополнительная помощь

Если проблема не решается, предоставьте:

1. **Логи Railway** (последние 50 строк)
2. **Ответ бота** на команду `/checkdb`
3. **Что происходит** когда отправляете `/editorder ID`
   - Бот не отвечает?
   - Бот выдает ошибку?
   - Бот отвечает, но не работает?

---

## ✅ Контрольный список

- [ ] Бот запущен на Railway
- [ ] Логи не показывают ошибок
- [ ] Файл `order-edit-manager.js` задеплоен
- [ ] Миграция БД выполнена (колонки добавлены)
- [ ] Команда `/editorder` есть в `bot.js`
- [ ] Бот отвечает на другие команды (`/start`, `/help`)
- [ ] Создана новая заявка (после обновления)
- [ ] Попробовали `/myorders` - работает?
- [ ] Попробовали `/editorder ID` - что происходит?

---

**Дата:** 08.02.2026  
**Статус:** В процессе диагностики
