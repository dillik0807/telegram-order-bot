# 🐘 Инструкция по переходу на PostgreSQL

## Преимущества PostgreSQL

- ✅ **Персистентное хранилище** - данные не теряются при деплое
- ✅ **Автоматические бэкапы** - Railway делает это автоматически
- ✅ **Простая настройка** - 1 клик в Railway
- ✅ **Production-ready** - подходит для продакшена
- ✅ **Масштабируемость** - легко увеличить мощность

---

## 📋 Пошаговая инструкция

### Шаг 1: Добавьте PostgreSQL в Railway

1. **Откройте ваш проект на Railway**
   - Перейдите на https://railway.app
   - Выберите ваш проект

2. **Добавьте PostgreSQL**
   - Нажмите "+ New" в правом верхнем углу
   - Выберите "Database"
   - Выберите "Add PostgreSQL"
   - Railway автоматически создаст базу данных

3. **Дождитесь создания**
   - Railway создаст базу данных (это займет 10-30 секунд)
   - Вы увидите новый сервис "PostgreSQL" в проекте

### Шаг 2: Скопируйте DATABASE_URL

1. **Откройте PostgreSQL сервис**
   - Нажмите на карточку "PostgreSQL"

2. **Перейдите в Variables**
   - Найдите переменную `DATABASE_URL`
   - Нажмите на иконку копирования
   - Значение будет выглядеть так:
     ```
     postgresql://postgres:password@host:5432/railway
     ```

### Шаг 3: Добавьте DATABASE_URL в сервис бота

1. **Откройте сервис бота**
   - Нажмите на карточку вашего бота

2. **Перейдите в Variables**
   - Нажмите "+ Add Variable" или "New Variable"

3. **Добавьте переменную**
   - **Variable:** `DATABASE_URL`
   - **Value:** (вставьте скопированное значение)
   - Нажмите "Add"

### Шаг 4: Установите зависимости

```bash
npm install
```

Это установит `pg` (PostgreSQL драйвер), который уже добавлен в `package.json`.

### Шаг 5: Мигрируйте данные (Опционально)

Если у вас уже есть данные в SQLite, перенесите их:

```bash
node migrate-to-postgres.js
```

Этот скрипт:
- ✅ Подключится к обеим базам данных
- ✅ Перенесет всех клиентов
- ✅ Перенесет все склады
- ✅ Перенесет все товары

**Примечание:** Заявки (orders) не переносятся, так как они временные.

### Шаг 6: Деплой

```bash
git add .
git commit -m "Add PostgreSQL support"
git push
```

Railway автоматически сделает деплой.

### Шаг 7: Проверка

После деплоя отправьте боту:

```
/checkdb
```

Вы должны увидеть:
```
🔍 Информация о базе данных:

🐘 Используется PostgreSQL
✅ Подключение активно

👥 Клиентов: X
🏬 Складов: X
🛒 Товаров: X
```

---

## 🔧 Как это работает

### Автоматический выбор базы данных

Код автоматически определяет, какую базу данных использовать:

```javascript
// В bot.js, admin.js и других файлах
const database = require('./database-wrapper');
```

`database-wrapper.js` проверяет:
- Если `DATABASE_URL` установлен → используется PostgreSQL
- Если нет → используется SQLite

### Локальная разработка

Для локальной разработки можно продолжать использовать SQLite:

1. **Не добавляйте DATABASE_URL в локальный .env**
2. Бот автоматически будет использовать SQLite
3. Для тестирования PostgreSQL локально:
   ```
   DATABASE_URL=postgresql://localhost:5432/testdb
   ```

---

## 📊 Сравнение SQLite vs PostgreSQL

| Характеристика | SQLite | PostgreSQL |
|----------------|--------|------------|
| Персистентность на Railway | ❌ Нет (без Volume) | ✅ Да |
| Автобэкапы | ❌ Нет | ✅ Да |
| Конкурентность | ⚠️ Ограничена | ✅ Отлично |
| Масштабируемость | ⚠️ Ограничена | ✅ Отлично |
| Настройка на Railway | ⚠️ Нужен Volume | ✅ 1 клик |
| Локальная разработка | ✅ Просто | ⚠️ Нужен сервер |
| Стоимость | ✅ Бесплатно | ⚠️ $5/месяц* |

*Railway предоставляет $5 бесплатных кредитов каждый месяц

---

## 🔍 Проверка работы

### Проверка 1: Подключение

```
/checkdb
```

Должно показать:
```
🐘 Используется PostgreSQL
✅ Подключение активно
```

### Проверка 2: Добавление данных

1. Добавьте тестового клиента:
   ```
   /addclient 999999999 | Тест | +992900000000
   ```

2. Проверьте список:
   ```
   📋 Список клиентов
   ```

3. Сделайте redeploy в Railway

4. Проверьте список снова:
   ```
   📋 Список клиентов
   ```

Клиент должен остаться! ✅

### Проверка 3: Склады и товары

1. Добавьте склад:
   ```
   /addwarehouse2 Тестовый Склад
   ```

2. Добавьте товар:
   ```
   /addproduct Тестовый Товар
   ```

3. Сделайте redeploy

4. Проверьте:
   ```
   /checkwarehouses
   📋 Список товаров
   ```

Все должно остаться! ✅

---

## ⚠️ Важные замечания

### 1. Миграция данных

Скрипт `migrate-to-postgres.js` переносит только:
- ✅ Клиентов
- ✅ Склады
- ✅ Товары

**Не переносятся:**
- ❌ Заявки (orders)
- ❌ Запросы на регистрацию

Это нормально, так как эти данные временные.

### 2. Локальная разработка

После перехода на PostgreSQL:
- Локально будет использоваться SQLite (если нет DATABASE_URL)
- На Railway будет использоваться PostgreSQL
- Это нормально и удобно для разработки

### 3. Стоимость

Railway предоставляет:
- $5 бесплатных кредитов каждый месяц
- PostgreSQL стоит ~$5/месяц
- Итого: **бесплатно** для небольших проектов

---

## 🐛 Решение проблем

### Проблема 1: "DATABASE_URL не установлен"

**Решение:**
1. Проверьте, что PostgreSQL создан в Railway
2. Проверьте, что DATABASE_URL добавлен в Variables сервиса бота
3. Сделайте redeploy

### Проблема 2: "Connection refused"

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен в Railway
2. Проверьте, что DATABASE_URL правильный
3. Подождите 1-2 минуты после создания PostgreSQL

### Проблема 3: "Данные не сохраняются"

**Решение:**
1. Проверьте логи Railway:
   ```
   Railway → Сервис бота → Deployments → View Logs
   ```
2. Ищите строку:
   ```
   🐘 Используется PostgreSQL
   ```
3. Если видите "📊 Используется SQLite" - DATABASE_URL не установлен

### Проблема 4: "Ошибка миграции"

**Решение:**
1. Убедитесь, что оба файла существуют:
   - `database.js` (SQLite)
   - `database-postgres.js` (PostgreSQL)
2. Проверьте, что DATABASE_URL установлен в .env
3. Запустите миграцию снова

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи Railway:**
   ```
   Railway → Deployments → View Logs
   ```

2. **Проверьте переменные окружения:**
   ```
   Railway → Variables
   ```
   Должна быть: `DATABASE_URL=postgresql://...`

3. **Проверьте через бота:**
   ```
   /checkdb
   ```

4. **Если ничего не помогает:**
   - Скачайте бэкап: `/downloaddb`
   - Пересоздайте PostgreSQL в Railway
   - Повторите настройку

---

## ✅ Чек-лист

- [ ] PostgreSQL создан в Railway
- [ ] DATABASE_URL скопирован
- [ ] DATABASE_URL добавлен в Variables сервиса бота
- [ ] `npm install` выполнен
- [ ] Данные мигрированы (если нужно)
- [ ] Код закоммичен и запушен
- [ ] Деплой успешен
- [ ] `/checkdb` показывает PostgreSQL
- [ ] Тестовые данные добавлены
- [ ] После redeploy данные сохранились

---

**Дата создания:** 7 февраля 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
