# 📦 Настройка Railway Volume для сохранения базы данных

## 🔴 Проблема

После каждого деплоя на Railway:
- ❌ Весь список клиентов исчезает
- ❌ Все заявки удаляются
- ❌ База данных обнуляется

**Причина:** SQLite база данных хранится в эфемерной (временной) файловой системе контейнера, которая удаляется при каждом деплое.

## ✅ Решение: Railway Volume

Railway Volume - это персистентное хранилище, которое сохраняется между деплоями.

---

## 📋 Пошаговая инструкция

### Шаг 1: Создайте Volume в Railway

1. **Откройте ваш проект на Railway**
   - Перейдите на https://railway.app
   - Выберите ваш проект с ботом

2. **Откройте настройки сервиса**
   - Нажмите на сервис бота
   - Перейдите в раздел "Settings" (⚙️)

3. **Создайте новый Volume**
   - Прокрутите вниз до раздела "Volumes"
   - Нажмите кнопку "+ New Volume"

4. **Настройте Volume**
   ```
   Mount Path: /app/data
   ```
   - Нажмите "Add"

5. **Подтвердите создание**
   - Volume будет создан автоматически
   - Вы увидите его в списке Volumes

### Шаг 2: Настройте переменные окружения

1. **Откройте раздел Variables**
   - В настройках сервиса перейдите в "Variables"

2. **Добавьте переменную DB_PATH**
   ```
   Variable: DB_PATH
   Value: /app/data/orders.db
   ```

3. **Сохраните изменения**
   - Нажмите "Add" или "Save"

### Шаг 3: Обновите код (уже сделано)

Код уже обновлен для поддержки Railway Volume:
- ✅ `database.js` - создает директорию автоматически
- ✅ Добавлены команды для управления БД

### Шаг 4: Деплой

1. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "Add Railway Volume support for database persistence"
   git push
   ```

2. **Railway автоматически сделает деплой**
   - Дождитесь завершения деплоя
   - Проверьте логи на наличие ошибок

### Шаг 5: Проверка

1. **Проверьте базу данных**
   - Отправьте боту команду: `/checkdb`
   - Вы должны увидеть путь: `/app/data/orders.db`

2. **Добавьте тестового клиента**
   - Используйте команду: `/addclient ID | Имя | Телефон`
   - Или одобрите запрос на регистрацию

3. **Сделайте redeploy**
   - В Railway нажмите "Redeploy"
   - Дождитесь завершения

4. **Проверьте, что данные сохранились**
   - Отправьте команду: `/checkdb`
   - Проверьте список клиентов: `📋 Список клиентов`
   - Клиент должен остаться в базе!

---

## 🔧 Новые команды для администратора

### `/checkdb` - Проверка базы данных

Показывает информацию о базе данных:
- 📁 Путь к файлу
- 📊 Размер файла
- 📅 Дата последнего изменения
- 👥 Количество клиентов
- 🏬 Количество складов
- 🛒 Количество товаров

**Пример:**
```
/checkdb
```

**Ответ:**
```
🔍 Информация о базе данных:

📁 Путь: /app/data/orders.db
✅ Файл существует
📊 Размер: 45.23 KB
📅 Изменен: 07.02.2026, 10:30:15

👥 Клиентов: 5
🏬 Складов: 3
🛒 Товаров: 8

💡 Используйте /downloaddb для скачивания резервной копии
```

### `/downloaddb` - Скачать резервную копию

Скачивает файл базы данных для резервного копирования.

**Пример:**
```
/downloaddb
```

**Результат:**
- Бот отправит файл `backup-2026-02-07T10-30-15.db`
- Сохраните этот файл в безопасном месте!

### `/uploaddb` - Загрузить базу данных

Восстанавливает базу данных из резервной копии.

**Пример:**
```
1. Отправьте команду: /uploaddb
2. Отправьте файл .db
3. Дождитесь подтверждения
4. Перезапустите бота или сделайте redeploy
```

**⚠️ ВНИМАНИЕ:** Это заменит текущую базу данных!

### `/reloaddata` - Обновить данные

Обновляет данные складов и товаров из базы данных.

**Пример:**
```
/reloaddata
```

---

## 🔍 Диагностика проблем

### Проблема 1: База данных все еще обнуляется

**Проверьте:**

1. **Volume создан?**
   ```
   Railway → Ваш проект → Сервис → Settings → Volumes
   ```
   Должен быть Volume с Mount Path: `/app/data`

2. **Переменная окружения установлена?**
   ```
   Railway → Ваш проект → Сервис → Variables
   ```
   Должна быть переменная: `DB_PATH=/app/data/orders.db`

3. **Проверьте логи Railway**
   ```
   Railway → Ваш проект → Сервис → Deployments → View Logs
   ```
   Ищите строки:
   ```
   📁 Создана директория для БД: /app/data
   📊 Путь к базе данных: /app/data/orders.db
   ✅ База данных подключена
   ```

4. **Проверьте через бота**
   ```
   /checkdb
   ```
   Путь должен быть: `/app/data/orders.db`

### Проблема 2: Ошибка "Permission denied"

**Решение:**

Railway автоматически настраивает права доступа к Volume. Если возникает ошибка:

1. Проверьте логи Railway
2. Убедитесь, что Mount Path правильный: `/app/data`
3. Попробуйте пересоздать Volume

### Проблема 3: База данных не создается

**Решение:**

1. Проверьте, что код обновлен (файл `database.js`)
2. Проверьте логи на наличие ошибок
3. Убедитесь, что директория `/app/data` доступна

---

## 💾 Резервное копирование

### Автоматическое резервное копирование

Рекомендуется регулярно скачивать резервные копии:

1. **Ежедневно:**
   ```
   /downloaddb
   ```
   Сохраняйте файл в безопасном месте

2. **Перед важными изменениями:**
   - Перед обновлением кода
   - Перед изменением структуры БД
   - Перед массовым удалением данных

3. **После важных событий:**
   - После добавления новых клиентов
   - После создания заявок
   - После изменения настроек

### Восстановление из резервной копии

Если данные были потеряны:

1. **Скачайте последнюю резервную копию**
   - Найдите файл `backup-*.db`

2. **Загрузите в бота**
   ```
   /uploaddb
   ```
   Отправьте файл .db

3. **Перезапустите бота**
   - Сделайте redeploy на Railway
   - Или перезапустите локально

4. **Проверьте данные**
   ```
   /checkdb
   📋 Список клиентов
   ```

---

## 📊 Мониторинг

### Регулярные проверки

Рекомендуется проверять базу данных:

1. **После каждого деплоя:**
   ```
   /checkdb
   ```

2. **Еженедельно:**
   ```
   /checkdb
   📋 Список клиентов
   ```

3. **При подозрении на проблемы:**
   ```
   /checkdb
   /downloaddb (для резервной копии)
   ```

### Признаки проблем

Обратите внимание, если:
- ❌ Размер БД = 0 KB
- ❌ Количество клиентов = 0
- ❌ Путь к БД не `/app/data/orders.db`
- ❌ Файл не существует

---

## ✅ Чек-лист настройки

- [ ] Volume создан в Railway (Mount Path: `/app/data`)
- [ ] Переменная `DB_PATH=/app/data/orders.db` установлена
- [ ] Код обновлен и задеплоен
- [ ] Команда `/checkdb` показывает правильный путь
- [ ] Тестовый клиент добавлен
- [ ] После redeploy клиент остался в базе
- [ ] Резервная копия скачана (`/downloaddb`)

---

## 📞 Поддержка

Если проблема сохраняется:

1. **Соберите информацию:**
   - Результат `/checkdb`
   - Логи Railway
   - Скриншот настроек Volume
   - Скриншот переменных окружения

2. **Проверьте:**
   - Volume создан и активен
   - Переменная `DB_PATH` установлена
   - Код обновлен
   - Деплой успешен

3. **Попробуйте:**
   - Пересоздать Volume
   - Сделать redeploy
   - Скачать и загрузить БД вручную

---

**Дата создания:** 7 февраля 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
