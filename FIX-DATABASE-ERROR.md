# ⚡ Быстрое исправление ошибки БД

## Ошибка
```
❌ Ошибка подключения к БД: SQLITE_CANTOPEN
```

## Причина
Бот настроен на SQLite, но работает на PostgreSQL (Railway/Heroku).

## Быстрое решение (3 команды)

```bash
# 1. Проверьте какая БД используется
node check-database.js

# 2. Переключитесь на PostgreSQL
node switch-to-postgres.js

# 3. Установите зависимости и запустите
npm install
npm start
```

## Что делает switch-to-postgres.js?

1. ✅ Создает резервную копию текущего database.js
2. ✅ Заменяет database.js на PostgreSQL версию
3. ✅ Добавляет `pg` в package.json
4. ✅ Добавляет DATABASE_URL в .env (если нет)

## Для Railway

Railway автоматически предоставляет `DATABASE_URL` при добавлении PostgreSQL сервиса.

**Шаги:**
1. Добавьте PostgreSQL в проект Railway
2. Выполните команды выше
3. Деплой

## Для локальной разработки

Добавьте в `.env`:
```env
DATABASE_URL=postgresql://postgres:password@localhost:5432/orders
```

## Проверка

После запуска вы должны увидеть:
```
✅ PostgreSQL подключен
✅ Таблицы PostgreSQL инициализированы
🤖 Бот запущен успешно!
```

## Возврат к SQLite

```bash
node switch-to-sqlite.js
```

## Помощь

Подробная инструкция: [POSTGRESQL-SETUP.md](./POSTGRESQL-SETUP.md)
